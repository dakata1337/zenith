fn sum(arr: int[]) -> int
    mut accum := 0
    for x in arr:
        accum += x
    end
    return accum
end

res := sum([1, 2, 3])

if res != 6
    print("what? how?")
else
    print("{}", res)
end
